# Issue #191 Saimaa stress fixture

This directory is reserved for the Saimaa lake relation stress fixture
(`relation/7379046`) requested for the geometry-validity regression suite.

The fixture is not enabled yet. It should be added after the Bangladesh #861
and Flevoland #697 harness is in place, with an `expected-fail` marker if the
current master cannot pass it before Stage A/Stage B geometry fixes.
